import { OpacityDirective } from './opacity.directive';

describe('OpacityDirective', () => {
  it('should create an instance', () => {
    const directive = new OpacityDirective();
    expect(directive).toBeTruthy();
  });
});
