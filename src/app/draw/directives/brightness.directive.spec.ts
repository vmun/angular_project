import { BrightnessDirective } from './brightness.directive';

describe('BrightnessDirective', () => {
  it('should create an instance', () => {
    const directive = new BrightnessDirective();
    expect(directive).toBeTruthy();
  });
});
